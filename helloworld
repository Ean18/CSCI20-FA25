#python hello world

hello = "world"

this_number = 52

this_number =int(this_number) + 5

this_number + 1

a = arr.array('h', #opcode for unsigned short [25])

b = arr.array('1', [1, 2, 3, 4, 5])

print(a.pop[])

print(this_number, 3)

# f : Num -> Num



def f(x): #define a funtion using "def" <name>(<import list>)
    return (x + 1) # must return some value

print(f(2), f(4), f(6))