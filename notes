-find devdocs.io
-search array in devdoc.io 

use array
-list (collection)
-type

what is an array
-an arrays is a sequence types and behave much like a list the objects are stored and compressed
-a sequence is a iterable which supports efficient element access using integer indices via __getitem__()
-a Getitem is a Called to implement assignment to self[key]
-a collection is a bunch of orgainzed in some meaning or not meaning full way
